config BR2_PACKAGE_GOOGLE_BREAKPAD
	bool "google-breakpad"
        depends on BR2_INSTALL_LIBSTDCPP
	depends on BR2_TOOLCHAIN_BUILDROOT_CONTEXT_FUNCS
	help
	  An open-source multi-platform crash reporting system

	  http://code.google.com/p/google-breakpad/

comment "google-breakpad requires a toolchain with C++ and context control functions"
	depends on !BR2_INSTALL_LIBSTDCPP || !BR2_TOOLCHAIN_BUILDROOT_CONTEXT_FUNCS
