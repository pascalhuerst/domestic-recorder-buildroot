From 2a6fa760a30c80674fa09354bbb842a06473ea10 Mon Sep 17 00:00:00 2001
From: Ross Burton <ross@linux.intel.com>
Date: Mon, 17 Aug 2009 14:50:52 +0100
Subject: [PATCH] Fix strrchr result checking

---
 libgupnp/gupnp-device-info.c |    2 +-
 1 files changed, 1 insertions(+), 1 deletions(-)

diff --git a/libgupnp/gupnp-device-info.c b/libgupnp/gupnp-device-info.c
index 17482e6..331dd9c 100644
--- a/libgupnp/gupnp-device-info.c
+++ b/libgupnp/gupnp-device-info.c
@@ -888,7 +888,7 @@ resource_type_match (const char *query,
 
         /* Inspect last colon (if any!) on @base */
         colon = strrchr (base, ':');
-        if (G_UNLIKELY (*colon == 0))
+        if (G_UNLIKELY (colon == NULL))
                 return !strcmp (query, base); /* No colon */
 
         /* Length of type until last colon */
-- 
1.6.0.4

