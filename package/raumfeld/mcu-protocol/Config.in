menuconfig BR2_PACKAGE_RAUMFELD_MCU_PROTOCOL
	bool "raumfeld-mcu-protocol"
	select BR2_PACKAGE_ALSA_LIB
	depends on BR2_arm
	help
	  ALSA Control Plugin and helper programs for Teufel MCU.

	  http://www.raumfeld.com/

if BR2_PACKAGE_RAUMFELD_MCU_PROTOCOL

config BR2_PACKAGE_RAUMFELD_MCU_PROTOCOL_REPOSITORY
        string "git repository that holds the mcu-protocol source code"
        default "ssh://git@sources.teufelhome.com/rfs/mcu-protocol.git"

config BR2_PACKAGE_RAUMFELD_MCU_PROTOCOL_VERSION
        string "version or branch to check out from the git repository"
        default "master"

endif
