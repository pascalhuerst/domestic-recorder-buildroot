--- usbmount-0.0.14.1-orig/usbmount	2007-01-27 12:22:14.000000000 +0100
+++ usbmount-0.0.14.1/usbmount	2010-08-05 09:12:04.000000000 +0200
@@ -40,7 +40,6 @@
 test -x /lib/udev/vol_id || { log err "cannnot execute /lib/udev/vol_id"; exit 1; }
 
 # Default values for configuration variables.
-MOUNTPOINTS=""
 FILESYSTEMS=""
 MOUNTOPTIONS=""
 FS_MOUNTOPTIONS=""
@@ -57,6 +56,7 @@
 if test "$1" = add; then
 
     # Acquire lock.
+    mkdir -p /var/run/usbmount/
     log debug "trying to acquire lock /var/run/usbmount/.mount.lock"
     lockfile-create --retry 3 /var/run/usbmount/.mount || \
 	{ log err "cannot acquire lock /var/run/usbmount/.mount.lock"; exit 1; }
