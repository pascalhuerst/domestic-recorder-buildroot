#!/bin/sh

case "$1" in
  start)
       # change the mount options for the rootfs to noatime
       mount -o remount,noatime /

       # tweak settings of the internal hard-disk:
       # - limit DMA mode to UDMA2 (33 MB/s)
       # - set the standby (spindown) delay
       hdparm -Xudma4 -S60 /dev/hda

       # start the master-process
       cd /raumfeld/master-process
       ./raumfeld-master-sh.sh &
       ;;
  *)
       ;;
esac

exit $?
