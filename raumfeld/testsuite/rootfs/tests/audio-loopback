#!/bin/sh

playback_device="hw:0,0"
capture_device="hw:0,1"

if [ -z "$(aplay -l | grep "Raumfeld DDX")" ]; then
    capture_device="hw:0,0"
fi

amixer cset numid=1 255
arecord -f cd - -D $capture_device | aplay -D $playback_device -f cd -

